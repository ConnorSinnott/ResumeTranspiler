version: '3'
services:
  builder:
    command: 'ts-node-dev --transpile-only --respawn src/main.ts'
    build: ./source/.
    working_dir: /home/node
    volumes:
      - ./source/src/:/home/node/src/
